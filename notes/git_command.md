Day1:
1️⃣ Create repo → git init
2️⃣ Tell Git who you are → git config
3️⃣ Tell Git where to push → git remote add
4️⃣ Select files → git add
5️⃣ Save snapshot → git commit
6️⃣ Send to GitHub → git push

config not done:
git init
git config --global user.name "sharmapandeyrohit-hue"
git config --global user.email "sharmapandeyrohit@gmail.com"
git remote add origin https://github.com/sharmapandeyrohit-hue/sms.git
git add .
git commit -m "Project Overview & Software Basics Setup"
git branch -M main
git push -u origin main


config already done once:
git init
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git add .
git commit -m "first commit"
git push -u origin main


